<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nabab.medipast">

    <uses-feature
        android:name="android.hardware.Camera"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/launcher_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WelcomeScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AddDoctorActivity"
            android:label="Add Doctor"
            android:theme="@style/AddedTheme" />
        <activity
            android:name=".ViewDoctorActivity"
            android:label="View Doctor" />
        <activity
            android:name=".AddMedicalHistoryActivity"
            android:label="Add Medical History" />
        <activity
            android:name=".ViewMedicalHistoryActivity"
            android:label="View Medical History" />
        <activity
            android:name=".ViewDoctorDetailsActivity"
            android:label="View Doctor Details" />
    </application>

</manifest>